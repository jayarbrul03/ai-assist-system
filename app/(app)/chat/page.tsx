import { PageShell, PageHeader } from "@/components/shared/page-header";

export default function ChatPlaceholder() {
  return (
    <PageShell>
      <PageHeader
        title="Rulebook AI"
        description="Phase 3 implements the streaming chat with RAG citations."
      />
    </PageShell>
  );
}
